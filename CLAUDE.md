# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Overview

Personal scripts and text files repo, synced to `git@github.com:dingtian808-spec/dtforcc.git` on the `main` branch.

## Files

- `alarm_clock.py` — Tkinter alarm clock (time only, no date)
- `alarm_clock_v2.py` — Tkinter alarm clock with date picker (year/month/day + hour/min/sec)
- `提示词` / `提示词v2` — Chinese text prompt files

## SSH / Git

SSH uses `ssh.github.com:443` to bypass port 22 blocking. Config is at `~/.ssh/config`.
